package com.rajdeep.model;

public class Hero extends Character {
    public Hero() { super(); }
    public Hero(String name, int level, int maxHealth, int power, int defense, int speed) {
        super(name, level, maxHealth, power, defense, speed);
    }
}
