package com.rajdeep.model;

public class Villain extends Character {
    public Villain() { super(); }
    public Villain(String name, int level, int maxHealth, int power, int defense, int speed) {
        super(name, level, maxHealth, power, defense, speed);
    }
}
